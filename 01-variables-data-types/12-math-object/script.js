let x; 

x = Math.sqrt(9); //the square root of a number

x = Math.abs(-6); // this is for the absolute number

x = Math.round(4.2);// the number roundup or round down

x = Math.ceil(4.2); //this will round any decimal up

x = Math.floor(4.9); // this will alway round a number decimal down

x = Math.pow(2, 3); // This contain two operations, the first is the number that want to be raised to the power of another

x = Math.min(4, 5, 3); // this shows the minimum value in a range

x = Math.max(4, 5, 3); // this shows the maximum value in a range

x = Math.random(); // generates a random number between 0 and 1

x = Math.random() * 10; // this will generate  random number from 0 to 9

x = Math.floor(Math.random() * 100 + 1); // this will generate content from 1 to 10;

console.log(x);